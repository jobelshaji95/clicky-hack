# Assets

Drop `clicky.ico` here to brand the executable and the system-tray icon, then
re-add `<ApplicationIcon>Assets\clicky.ico</ApplicationIcon>` to `Clicky.csproj`.

When this file is absent the tray falls back to the default system application
icon, so the app still runs without it.
